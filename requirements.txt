# Versions used for the reported Windows/Python 3.10 runs.
# Install a PyTorch wheel matching the local CUDA driver when the default index
# does not provide the required CUDA build (the reported run used 2.10.0+cu128).
torch==2.10.0
numpy==1.26.4
scipy==1.15.3
scikit-learn==1.7.2
h5py==3.16.0
librosa==0.11.0
transformers==5.12.1
matplotlib==3.8.4
tensorboard==2.20.0
